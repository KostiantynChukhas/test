//

import Foundation

typealias EmptyClosureType = () -> ()
typealias SimpleClosure<T> = (T) -> ()

struct Defines {
        static let baseUrl = URL(string: "https://496.ams3.cdn.digitaloceanspaces.com/")!
}
